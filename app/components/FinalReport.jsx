import React from "react";
import { Text, StyleSheet } from "react-native";
const FinalReport = () => {
  return <Text>Final Report</Text>;
};

export default FinalReport;

// This component to submit the final report, accessed only after the operator signals the end of the
// intervention, and it must include: whether the issue was resolved or not, what was
// accomplished, if there is a need for further interventions and, if so, what interventions
// are these.
